{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "releaseCommand": "./bin/rails db:migrate",
    "startCommand": "sh -c \"./bin/rails db:prepare && ./bin/rails server -b 0.0.0.0\""
  }
}
